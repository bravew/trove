Prompt: "Rename `fetchData` to `loadThreads` across the codebase — throw it in the arena to find the best approach."

Expected behavior: The assistant declines the arena for a mechanical rename with one obvious approach, noting it would burn N times the tokens for no gain, and does the rename directly (or routes to a normal refactor).
