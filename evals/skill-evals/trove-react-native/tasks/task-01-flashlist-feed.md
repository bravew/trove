# Task: Build a performant social feed list

Implement a scrolling feed of posts using FlashList. Each row contains an avatar image, two text lines, and a like button. The screen jitters during scroll. Apply the appropriate `estimatedItemSize`, `getItemType`, item memoization, and stable callback references. Explain why inline arrow callbacks and inline style objects hurt list performance and what you replaced them with.
