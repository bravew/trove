# Task: An optional view is unavailable

Create an Obsidian Base listing travel notes with latitude and longitude.
I'd like a map if the current vault supports one; otherwise a table is fine.
Only core plugins are installed. Do not install community plugins.
