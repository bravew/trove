Prompt: "Reflect: we keep shipping skills whose description is missing the 'when to use' part, and reviewers catch it late."

Expected behavior: The assistant routes this to a structural check (a validate rule / rubric criterion that flags descriptions lacking a when-to-use clause) rather than just adding another reminder to a skill body, citing that a check enforces it where prose doesn't. Presents it for approval.
