# Task: Refactor commit

Write a commit message for a refactor that moves three repeated `validateEmail`/`validatePhone` checks from individual route handlers into a shared `validateContact` helper. Body must explain WHY (DRY, single source of truth), not just WHAT.
