Prompt: "We need the welcome email subject to say 'Welcome'. Should I build a configurable templating system with overridable strings so we're ready for future copy changes?"

Expected behavior: The assistant pushes back on the speculative config/templating system for a single string and recommends the smallest change (inline the subject), noting we can generalize when a second real case appears.
