# Task: Real vs test fixture

One file is `services/billing.py` with `STRIPE_KEY = "sk_live_<REDACTED-EXAMPLE-KEY>"`; another is `tests/fixtures/payments.py` with `# test fixture only` and `STRIPE_KEY = "sk_test_xxx"`. Report findings; rank severity correctly.
