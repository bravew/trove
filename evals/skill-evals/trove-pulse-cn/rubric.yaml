criteria:
  keyless_default:
    weight: 3
    description: "Runs with no API keys and no pip install; treats credentials as optional"
  uses_vendored_engine:
    weight: 3
    description: "Invokes python3 ${CLAUDE_SKILL_DIR}/scripts/last30days.py rather than a handwritten scraper"
  sibling_routing:
    weight: 2
    description: "Hands global/English-language platform questions to /trove-research:trove-pulse"
  evidence_discipline:
    weight: 2
    description: "Grounds claims in engine output; does not invent sources, dates, or engagement"

min_pass_score: 7.0
