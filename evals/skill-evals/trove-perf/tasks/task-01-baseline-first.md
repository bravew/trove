Prompt: "The /threads endpoint feels slow. Make it faster."

Expected behavior: The assistant captures a baseline measurement (request timing and/or query plan, saved as an artifact) and locates the actual cost before changing code, rather than immediately rewriting based on a guess.
