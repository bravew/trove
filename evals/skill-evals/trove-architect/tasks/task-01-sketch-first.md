Prompt: "Add a new 'ResearchAgent' to the backend agent factory that can call external search tools and hand results to the GeneralAgent. Start coding."

Expected behavior: The assistant first sketches the shape — the agent's type/interface, its tool signatures, and how it slots into the factory's selection — with not-implemented bodies, before writing logic. It names the data shapes (agent state, tool result) up front.
