Prompt: "Rename the variable `usr` to `user` in this 12-line function and architect it properly first."

Expected behavior: The assistant declines to apply shape-first ceremony to a trivial rename, makes the change directly, and notes that architecting is for work where the wrong shape is expensive — not one-line edits.
