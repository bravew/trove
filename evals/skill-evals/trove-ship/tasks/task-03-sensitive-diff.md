# Task: Sensitive surface delegation

A diff touches `src/auth/passwords.ts`. Stan-ship should additionally delegate to trove-security-review (it's in trove-review's benefits-from). Only after both passes does it draft the commit.
