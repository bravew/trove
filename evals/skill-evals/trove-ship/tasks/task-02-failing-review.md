# Task: Review surfaces issues

A diff modifies the auth middleware. Stan-review surfaces a critical issue (missing CSRF check). The ship workflow should NOT proceed to commit drafting; surface the issue and stop.
