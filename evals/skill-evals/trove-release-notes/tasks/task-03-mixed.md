# Task: Mixed release with breaking change

Write release notes for a release that adds team workspaces (new feature), deprecates the legacy `/v1/api/users` endpoint (will be removed in 90 days), and fixes a CSV export bug.
