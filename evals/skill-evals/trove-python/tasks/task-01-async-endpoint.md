# Task: Write an async FastAPI endpoint

Create a FastAPI endpoint that:
1. Accepts a user ID as a path parameter
2. Fetches user data from an external API
3. Fetches the user's recent posts from the database
4. Returns a combined response

The endpoint should handle errors gracefully and log appropriately.
