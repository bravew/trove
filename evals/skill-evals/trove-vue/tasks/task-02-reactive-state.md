# Task: Reactive state

Write a Vue component that maintains a counter (starts at 0) with increment/decrement buttons, and shows whether the count is even or odd as a computed value. Use `ref`/`computed` from the Composition API.
