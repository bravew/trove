# Task: Vue 3 component

Write a Vue 3 single-file component (`UserCard.vue`) that takes `name` and `email` as required props (typed) and emits a `click` event when the card is clicked. Use `<script setup>` and the Composition API.
