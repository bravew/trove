# Task: Pinia store

Define a Pinia store named `useUserStore` with state `{ user: null }`, an action `setUser(u)`, and a getter `isLoggedIn`. Use the Composition API setup-function form, not the options form.
