# Task: Implement directional navigation transitions

Add forward / back directional transitions to a wizard flow: forward navigation slides in from the right; back navigation slides in from the left. Use `addTransitionType()` to tag transitions and CSS to branch on the type. Explain how you detect direction (history index, intent flag, or popstate) and why the type-based approach beats writing two separate transition setups.
