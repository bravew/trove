# Task: Animate a shared element across routes

A list page shows post thumbnails; clicking one navigates to a detail page where the same image becomes the hero. Implement a shared-element morph using `view-transition-name`, ensuring the name is unique per post and applied on both pages. Explain why two simultaneous list-item transitions with the same name would break, and how you scoped names per item.
