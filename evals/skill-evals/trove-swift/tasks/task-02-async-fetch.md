# Task: Async data fetch

Write an async `fetchUsers()` method on `UserService` that returns `[User]`. Handle network errors with `throws` rather than swallowing. Show how the caller awaits and updates UI.
