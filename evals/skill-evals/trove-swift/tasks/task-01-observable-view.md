# Task: SwiftUI view-model

Write a `UserProfileViewModel` Swift 6 view model using `@Observable` that loads a `User` async from a `UserService` and exposes a `loadingState: LoadingState` enum. Show how a SwiftUI `View` consumes it.
