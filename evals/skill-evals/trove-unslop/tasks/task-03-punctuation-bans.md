Prompt: Fix the punctuation tells: "The migration is straightforward — we move callers first, then delete the old API. The tradeoff is clear: speed versus safety, and we chose speed."

Expected behavior: The assistant removes the em dash (splitting into two sentences) and the mid-sentence colon connector, producing plain sentences while keeping the meaning. It does not introduce a parentheses substitute for the em dash.
